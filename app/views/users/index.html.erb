<% content_for(:html_title) { 'タスク' } %>
<section class="hero">
<div class="container">
    <div class="row">
    <div class="col-lg-11 col-md-11 col-sm-12 mx-auto">
        <div class="letter">
            <div class="all-position">
                <div class="row">
                    <div class="col-md-5">
                        <%= render "sorcery" %>
                        <div class="left-page">
                        <div class="left-page-position2">
                            <%= render 'status' %>
                        </div>
                        <div class="charactor-position" >
                            <%= render 'hukidasi2' %>
                            <%= image_tag "task_boy.png", :size =>'200x260', class: "boy" %>
                        </div>
                        </div>
                    </div>

                    <div class="col-md-7">
                        
                            <div class="light_page_top">
                                <%= render 'long_question', user: @user%>
                                <%= render 'firsts_question'%>
                                <%= render 'second_question'%>
                                <%= render 'third_question' %>
                                <%= render 'recovery_question', recovery_question: @recovery_question, recovery_answers: @recovery_answers %>
                            </div>
                        
                    </div>

                    <%= render "modal" %>
                </div>
            </div>
        </div>
    </div>
    </div>
</div>
</section>

